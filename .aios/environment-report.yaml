# AIOS Environment Report
# Generated by environment-bootstrap task
generated_at: "2026-03-31"

project:
  name: meu_projeto_AIOS
  github_owner: jrthurler
  github_repo: https://github.com/jrthurler/meu_projeto_AIOS
  git_initialized: true
  initial_commit: true

platform:
  os: macOS
  arch: x86_64
  shell: bash

tools:
  essential:
    git:
      status: installed
      version: "2.39.5"
    gh:
      status: installed
      version: "2.89.0"
      path: ~/bin/gh
      authenticated: true
      account: jrthurler
    node:
      status: installed
      version: "v24.14.1"
    npm:
      status: installed
      version: "11.11.0"

  infrastructure:
    supabase:
      status: not_installed
      note: "npm install failed on Node 24; needs Homebrew"
    railway:
      status: not_installed
    docker:
      status: not_installed
      note: "Docker Desktop required — manual install"

  quality:
    coderabbit:
      status: not_installed
      note: "Install script URL unavailable; revisit manually"

  optional:
    pnpm:
      status: installed
      version: "10.33.0"
    bun:
      status: installed
      version: "1.3.11"
      path: ~/.bun/bin/bun

aios:
  status: installed
  version: "4.4.6"
  package: "@aios-fullstack/pro"
  bin: node_modules/.bin/aios

bootstrap_status: partial
notes:
  - gh installed to ~/bin — add to PATH via export PATH="$HOME/bin:$PATH" in ~/.zshrc
  - bun installed to ~/.bun/bin — needs shell restart or source ~/.bash_profile
  - Homebrew not installed — needed for supabase, docker via brew
  - Repository linked to existing GitHub remote
